Fix the AI insight caching in src/components/BodyBattery48hDialog.tsx:

1. Key on data values, not on open/wakeTimeIso. Replace line 344's key with: const key = `${truth.percent}|${truth.hoursAwake.toFixed(1)}|${truth.drainAwake}|${truth.drainActive}|${truth.startPercent}`; Leave the early-return (if (insightKeyRef.current === key) return;) and move insightKeyRef.current = key from line 346 (pre-call) into the .then/.catch blocks so a failed call can be retried, and only mark "fetched" on actual completion.
2. Stop wiping the cache on close. Remove the insightKeyRef.current = null; line inside the reset effect (lines 333-339). Keep the setInsight({ loading: false, text: null }) behaviour out of the reset too - instead, on reopen the existing insight text should remain visible immediately if the key still matches. Simplest approach: delete the entire reset effect; the fetch effect will either skip (cached) or refetch (key changed) on its own. The cached insight state survives because the component stays mounted (Dialog hides via open prop). If verification shows the component actually unmounts on close (Radix Dialog by default keeps it mounted, so state persists), no extra work needed. If it does unmount, the cache lives only across same-mount reopens - still an improvement.
3. No changes to payload, pattern detection, fallback text, or dependency array ([open, user, truth, points, readinessData, prevSleep]).

Acceptance: Open at 21:19 fetches insight, caches under e.g. 50|13.3|26|0|95. Close, reopen at 21:25 with identical truth values, effect runs, key matches, returns early, text shown instantly with no network call. Battery drops to 47% or hour rolls over (changing drainAwake), key changes, edge function is called again. Failed call leaves insightKeyRef.current unchanged, so the next open retries.

Files: src/components/BodyBattery48hDialog.tsx (lines approximately 333-346)